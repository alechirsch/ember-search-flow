<Input
	@class="search-flow_input"
	@value={{this.value}}
	@placeholder={{this.placeholder}}
	@focusOut={{action 'inputBlurred'}}
	@focusIn={{action 'inputFocused'}}
	@keyDown={{action 'inputKeyDown'}}
	@keyUp={{action 'inputKeyUp'}}
	@enter={{action 'inputEntered'}}
/>
{{#if this.filter.isFocused}}
	<SearchFlow::Dropdown
		@options={{this.availableOptions}}
		@activeOption={{this.activeOption}}
		@isContains={{this.filter.isContains}}
		@isLoading={{this.isLoading}}
		@selectOption={{action 'selectOption'}}
	/>
{{/if}}